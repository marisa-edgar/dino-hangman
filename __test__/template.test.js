import Template from "../src/js/template.js";

describe("Template", () => {
  
  let template;
  
  beforeEach(() => {
    template = new Template();
  });
  
  test("should correctly create a template object", () => {
    expect(template.property).toEqual();
  });
}):
